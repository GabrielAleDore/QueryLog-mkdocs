# Indeces dos aquivos de script


```sql linenums="1"
SELECT 
    *
FROM
    NOME_TABELA
```


=== "No WinSQL"
    ```SQL linenums="1"
    SELECT 
        * 
    FROM 
        NOME_TABELA
    ```

=== "No Terminal"
    ```SQL
    db2 "SELECT * FROM NOME_TABELA"
    ```